export default function SimplePage() {
  return (
    <div style={{ padding: '40px' }}>
      <h1>Simple Test Page</h1>
      <p>This page should show the global header from root layout.</p>
      <p>If you can see the header here, then the global layout is working.</p>
    </div>
  )
}